## Why

The 導入策略 slide uses corporate jargon (Pilot/Team/Org scope labels, "跨職能小隊", "review gate", "回灌機制") that obscures a simple three-step message for a casual demo audience. Every term on the slide requires explanation, making the slide a liability instead of a punchline.

## What Changes

- Replace the three `.stage-card` visual elements (Pilot / Team / Org) with a plain-language numbered flow: ① 試跑一個專案 → ② 推給組員 → ③ 固定成習慣
- Remove all jargon: no "Pilot", "Team", "Org", "跨職能小隊", "review gate", "回灌"
- Keep the existing slide structure (`.workflow-band` + `.copy`) — only the text content changes
- The heading and body copy in `.copy` may be lightly adjusted for consistency with the new plain-language tone

## Capabilities

### New Capabilities

### Modified Capabilities

## Impact

- `presentation.html`: text content of the 導入策略 slide's `.visual` area rewritten; `.copy` heading/body optionally adjusted
